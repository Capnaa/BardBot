# Copy to .env and fill in. That file is gitignored; keep it that way.
# The token is the only secret the bot has. If it is ever committed or pasted anywhere
# shared, regenerate it in the Discord developer portal. Deleting the message is not enough.
DISCORD_TOKEN=
